# ZettaNote Environment Configuration
# Copy this file to /opt/zettanote/shared/.env on your server
# DO NOT commit the actual .env file to Git!

# Backend Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
# Local MongoDB
DB=mongodb://localhost:27017/zettanote

# Or MongoDB Atlas (cloud)
# DB=mongodb+srv://username:password@cluster.mongodb.net/zettanote?retryWrites=true&w=majority

# JWT Secret Key
# Generate a secure random key with: openssl rand -base64 32
# IMPORTANT: Use a strong, random secret in production!
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY_USE_openssl_rand_base64_32

# Optional: Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: CORS Configuration
# CORS_ORIGIN=https://your-domain.com
